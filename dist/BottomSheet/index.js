import { BottomSheet, } from '@react-native-elements/base/dist/BottomSheet/BottomSheet';
import { withTheme } from '../config';
export { BottomSheet };
export default withTheme(BottomSheet, 'BottomSheet');
